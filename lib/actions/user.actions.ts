'use server'

export const signIn = async () => {
   try{
       // mutation / Database / Make fetch 
   } catch (error) {
      console.error('Error')
   }
}


export const signUp = async (userData: SignUpParams) => {
   try{
       // mutation / Database / Make fetch / Creating a user
   } catch (error) {
      console.error('Error')
   }
}